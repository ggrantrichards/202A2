To run the CKY, simply
1. run 'pip3 install tabulate'
2. Then, you can run 'python3 cky.py'

This will output the results of the CKYParse as well as
the results of the weightedCKY, which will print the inside probabilities and
also the amost probable parse trees